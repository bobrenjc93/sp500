# Quote Previews

Each file in this folder is a browser-friendly preview of the matching archive in `../by_year/`.

- `2025.csv` is the first 200 rows from `../by_year/2025.csv.gz` after sorting by `date`, `symbol`, and `member_id`.
- The previews are regenerated by `scripts/update_data.py`.
